const linhaDeChegada = 480;
const dias = '';
const metros = '';
const resultado = '';

    function somaAPrimeiraTartaruga (dias, metros) {
            metros = 30;
            dias = 16;

        if (metros * dias === linhaDeChegada ) {
            console.log(`A primeira tartaruga percorreu ${linhaDeChegada} metros, em ${dias} dias até a linha de chegada !`);
        }
    }

    somaAPrimeiraTartaruga(dias, metros);

    function quantoTempoASegundaTartarugaLevou (metros, resultado) {
            metros = 20;

            if  (metros / linhaDeChegada === 24) {
                resultado =  24 - 16 
                }
          console.log(`A segunda tartaruga levou ${resultado} dias a mais que a primeira tartaruga para finalizar a corrida ! `)  
        }

    quantoTempoASegundaTartarugaLevou();









